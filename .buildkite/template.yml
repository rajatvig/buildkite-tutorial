name: "Buildkite Tutorial by Stark & Wayne"
description: |
  Learning to :pipeline: is odd.
steps:
- label: ":pipeline:"
  command: buildkite-agent pipeline upload .buildkite/pipeline.show-environment.yml
